package test.data;

/**
 * Sử dụng khi định nghĩa cho một lớp nào đó
 * khi sử dụng phương thức static create của lớp thì nêu gọi init để định nghĩa cho lớp đó
 */
public interface InitProtocol {
    abstract boolean init();
}
